# HTML-ZtEdit

> 专为「手写原生 HTML 视觉页」打造的可视化编辑器。直接编辑真实 DOM（不是组件物料模型），因此你页面里的 CSS 动画、绝对定位布局、字幕、拼贴图都会原样保留。

> 📋 项目来龙去脉、双环工作流、换环境恢复指南与完整路线图：见 [WORKFLOW.md](WORKFLOW.md)。

本项目起源于一个真实需求：用户有一份约 28 屏的自动播放 HTML 视觉页（纯原生 HTML + CSS `@keyframes` 动画 + 1 段自动播放脚本），希望对其中控件做拖拽、对齐、改样式等操作。通用低代码引擎（如 Rxdrag）只认自己的 JSON schema、解析不了任意 HTML，于是本项目参考其「画布 + 选中 + 通信」思路，针对原生 HTML 重写了一套轻量编辑器。

---

## 核心特点

- **直接改真实 DOM**：所见即所得，不引入物料模型；原有 CSS 动画、布局、字幕结构全部保留。
- **本地运行，绝不联网**：文件夹、图片、视频全部本地读取，永不上传任何服务器。
- **iframe 沙箱承载被编辑页面**：父窗口（React）与 iframe 通过 `postMessage` 通信，二者样式隔离，互不污染。
- **编辑态冻结 CSS 动画**：拖拽/选中时不会被页面自身的动画干扰。
- **字幕驱动的讲解型视觉页**：支持数组字幕与 DOM 字幕、字幕↔元素绑定、聚焦强调动画、导出自动播放脚本。

---

## 功能特性

### 加载与项目管理

| 能力 | 说明 |
| --- | --- |
| 选择文件夹 | 选中包含 HTML 及其图片/视频的整个文件夹，自动读取同目录依赖 |
| HTML 多文件 | 文件夹内多个 HTML 可在顶栏下拉切换，各自加载/编辑/导出 |
| 本地草稿 | 编辑进度自动存到本机 IndexedDB，刷新后自动恢复；可「清除草稿」 |

### 画布视图

| 能力 | 说明 |
| --- | --- |
| 模拟分辨率 | 顶栏下拉模拟 1080P / 2K / 4K 或「当前屏幕」「编辑器窗口」，查看元素与屏幕的占比（自动换算 CSS / 物理像素） |
| 画布缩放 | 按住 `Ctrl + 滚轮` 缩放画布（50% ~ 150%，10% 步进），顶栏显示当前百分比 |
| 网格 | 网格开关，拖动时自动吸附对齐 |

### 选中与排版

| 能力 | 说明 |
| --- | --- |
| 单选 / 拖动 | 画布内点选任意元素（图片 / 卡片 / 文字 / 字幕）；用 `transform: translate` 平移，**不重排兄弟元素** |
| 多选 | 按住 `Ctrl` 点击多选，多选后可**整组一起拖动** |
| 框选 | 空白处拖拽框选；`Shift/Ctrl` 连布局容器一起选，`Alt` 碰边即选 |
| 调整大小 | 选中元素出现 8 个手柄（四角 + 四边），拖拽调整宽高；`Shift` 或「锁定纵横比」保持比例 |
| 对齐 / 分布 / 等尺寸 | 左对齐(L)、水平居中(C)、右对齐(R)、顶端对齐(T)、垂直居中(M)、底端对齐(B)、横向分布(H)、纵向分布(V)、等高(E)、等宽(W)、等尺寸(Q) |
| 智能参考线 | 拖动 / 缩放大小时，自动吸附到其他元素边缘 / 中线（类似 PPT / PS 的延长线吸附） |
| 图层 | 顶栏 置顶 / 上移 / 下移 / 置底 调整层级 |
| 组合 / 取消组合 | 多选后组合成组，可整体移动 / 选中 / 拆开 |
| 锁定 | 锁定元素后不可拖动 / 修改（顶栏「锁定 / 解锁」） |
| 键盘微调 | 方向键按 1px 微调，`Shift + 方向键` 按 10px |
| 重置选中 | 一键清除选中元素的内联样式，恢复页面原始生效 |

### 属性编辑

| 能力 | 说明 |
| --- | --- |
| 属性面板 | 右侧「属性」Tab，改 宽度 / 高度 / 文字色 / 背景色 / 字体 / 字号 / 字重；面板随选中元素同步回显 |
| 边框 / 圆角 / 阴影 | 粗细、样式（实线/虚线/点线/双线）、颜色、圆角、阴影预设（轻微/中等/较重） |
| 文字编辑 | **双击**文字元素进入编辑（改 / 删），点击其他区域提交 |
| 锁定纵横比 | 属性面板可勾选，改宽自动带高、改高自动带宽 |
| 信息面板 | 右侧「信息」Tab 展示标签 / class / id / 定位 / transform / 宽高 / 字号字重等原始信息 |

### 素材

| 能力 | 说明 |
| --- | --- |
| 素材面板 | 右侧「素材」Tab 列出选中文件夹内的图片 / 视频缩略图 |
| 点击放置 | 点素材 → 画布出现十字光标 → 点画布插入 |
| 拖拽放置 | 从素材面板直接拖入画布，或从系统文件管理器拖文件入画布 |
| 智能布局 | 插入图片时自动识别附近图片容器并入列；素材自动加圆角 + 阴影 |
| 图片替换 | 选中图片后可通过素材替换 |
| 导出还原 | 素材保持相对路径引用，导出后仍可独立播放 |

### 字幕 / 时间轴 / 绑定 / 动画（讲解型视觉页）

| 能力 | 说明 |
| --- | --- |
| 底栏时间轴 | 显示当前页字幕块（DOM 字幕=红色、全局数组字幕=橙色、已绑定=绿色），点击可选中 |
| 页码导航 | 底栏 上一页 / 下一页 切换 `.slide` 页面 |
| 字幕跨页 | 选中字幕可「移动到上一页 / 下一页」；DOM 字幕直接移动元素，数组字幕调整 `slideTimings` 时间边界 |
| 字幕绑定 | DOM 字幕（`data-zt-role="subtitle"`）可绑定到画面元素：选中字幕 → 绑定 → 点画布元素 → 确认 |
| 绑定联动 | 已绑定元素带绿色标记；选中字幕时高亮其绑定元素（选中后可直接改样式/动画） |
| 入场动画 | 为元素设置 放大 / 缩小 / 淡入 / 飞入各方向 / 弹跳 / 旋转，可设时长、延迟、恢复时长、缓动 |
| 聚焦强调 | 元素设为「聚焦放大」后，字幕触发时**同组其他元素变暗、目标放大高亮**（`focus-group`） |
| 动画即时预览 | 下拉 / 调参时自动播放预览 |
| 全局字幕 | 支持页面脚本内 `subtitles[]` + `slideTimings[]` 数组字幕（橙色块）自动读取与展示 |

### 撤销 / 重做 / 复制

| 能力 | 说明 |
| --- | --- |
| 撤销 / 重做 | `Ctrl+Z` 撤销、`Ctrl+Shift+Z`（或 `Ctrl+Y`）重做，覆盖位置 / 尺寸 / 样式 / 文字 / 增删 / 绑定 / 移动 |
| 复制粘贴 | `Ctrl+C` 复制、`Ctrl+V` 粘贴，**可跨页**复制粘贴元素 |

### 导出

- 一键导出 `edited.html`：资源恢复相对路径、自动播放脚本还原、开场页 `active` 正确重置。
- **自动生成播放脚本**：导出时若原页面有 `slideTimings` 数组，会重新生成一套播放脚本 —— 从 DOM 读取字幕与绑定，按 `slideTimings` 时间轴自动翻页、随字幕触发绑定元素的**入场动画**或**聚焦强调**，并支持方向键 / 点击切页、手动翻页后自动暂停数秒。
- 编辑器足迹（`zt-*` 选择类、`zt-focus-active`、`dim-others` 等）在导出时自动剥离，保证导出页干净。

### 录屏（桌面端）

| 能力 | 说明 |
| --- | --- |
| 一键录制 | 勾选顶栏「录屏」后点播放即开始录制；停止播放或音频播完自动保存视频 |
| 输出 MP4 | **OBS 浏览器源捕获**（系统级），H.264 + AAC，成片可直接进剪映 / PR |
| 分辨率与窗口无关 | 主进程自动拉起 OBS，用固定尺寸的浏览器源离屏跑时间轴，编辑器窗口拖多小都不影响成片 |
| 分辨率档位 | 顶栏可切换 1080P / 2K / 4K |
| 音轨 | 取自编辑器内正在播放的页面；离屏页静音，不会出现双声源 |
| 浏览器兜底 | 纯浏览器模式（非 Electron）拿不到资源根目录，自动退回「捕获编辑器窗口 + 裁剪画布」方案，输出 webm、分辨率随窗口 |

---

## 技术栈

- React 18 + Vite 5
- Electron 31（Chromium 126）桌面壳 —— 提供文件夹级资源读取与 OBS 系统级 MP4 录屏
- 纯前端，无后端依赖
- 编辑器以 `iframe` 沙箱承载被编辑页面，父窗口（React）与 iframe 通过 `postMessage` 通信
- 草稿持久化：`IndexedDB`

## 目录结构

```
HTML-ZtEdit/
├── 打包.bat            # 一键打包 Windows 桌面 exe（双击：装依赖 + Electron 打包，已内置国内镜像）
├── index.html          # 应用入口
├── vite.config.js      # Vite 配置（base 设为相对路径，便于分发）
├── package.json
├── src/
│   ├── main.jsx          # 挂载 React
│   ├── App.jsx           # 主界面：工具栏 / iframe 画布 / 属性面板 / 时间轴 / 通信
│   ├── editorRuntime.js  # 注入 iframe 的纯脚本：选中 / 拖动 / 缩放 / 网格 / 对齐 / 绑定 / 动画 / 导出
│   ├── loadFolder.js     # 文件夹选择、相对路径解析
│   ├── htmlProcess.js    # 剥离脚本 / 资源重写 blob / 导出还原 / 自动播放脚本生成
│   ├── draftStore.js     # IndexedDB 草稿自动存 / 恢复
│   ├── animEffects.js    # 动画效果名常量表（编辑器与导出共用）
│   └── index.css
├── electron/           # Electron 桌面壳（入口 / 录屏引擎 obsRecorder / 开发调试）
│   ├── main.cjs           # 主进程：本地静态服务 / IPC / 文件选取
│   ├── preload.cjs        # 预加载桥：暴露 ztPick / ztRoot / ztSave / OBS 控制
│   ├── obsRecorder.cjs    # OBS 系统级自动录制（浏览器源捕获，MP4 输出）
│   └── dev-runner.cjs     # 开发调试启动器（Vite + Electron 联动）
├── scripts/            # 辅助脚本（契约校验 / 动画回归 / 字幕转换 / mp4 诊断）
├── start-dev.bat       # 一键启动调试（检查依赖 → 国内镜像 → 起 dev:electron）
├── stop-dev.bat        # 一键释放调试进程（清 5173-5179 端口 + 杀 electron）
├── 打包.bat            # 一键打包 Windows 安装包（内置国内镜像）
├── dist/               # Vite web 构建产物
└── dist-electron/      # Electron 打包产物
```

---

## 快速开始（本地开发）

> 前置：Node.js LTS 18+，并建议使用 npm。

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器（默认 http://localhost:5173）
npm run dev
```

用浏览器打开 `http://localhost:5173` 即可。

### 桌面端调试

双击 `start-dev.bat`（或 `npm run dev:electron`）进入桌面端调试：

- **F12** 或 **Ctrl+Shift+I**：开合 DevTools
- `src/` 下的代码改完自动热更新；改 `electron/` 主进程代码**需重启调试会话**
- 进程残留导致端口占用时，运行 `stop-dev.bat` 一键释放

> `.bat` 工具说明：`start-dev.bat` 负责一键启动（检查 node/npm → 缺依赖时自动 `npm install`（国内镜像）→ 确保 Electron 运行时 → 启动 `dev:electron`，启动前会自动清理 5173-5179 残留端口）；`stop-dev.bat` 负责清理（释放 5173-5179 端口 + 结束 `electron.exe`）。两者都是纯 cmd 实现，**不依赖 PowerShell**（避免中文路径传给 `powershell -File` 不可靠的坑），脚本本身保持 ASCII 编码。

## 一键打包（Windows 桌面程序）

直接**双击项目根目录的 `打包.bat`** 即可，它会自动完成三步：

1. 设置国内镜像（`npm` 源 + `Electron` 二进制镜像 + 辅助二进制镜像），避免公司代理下从 GitHub 下载卡死；
2. `npm install` 安装依赖；
3. `npm run electron:build` 打包成 `dist-electron/HTML-ZT-Edit Setup *.exe`。

> 这些镜像与「跳过证书校验」环境变量只在本次 bat 运行内生效，不会修改你全局的 npm 配置。

`dist/` 为 web 构建产物（可部署静态站点）；`dist-electron/` 为桌面安装包。

Linux / macOS 或喜欢命令行的用户，手动执行：

```bash
export npm_config_registry=https://registry.npmmirror.com
export ELECTRON_MIRROR=https://registry.npmmirror.com/-/binary/electron/
export ELECTRON_BUILDER_BINARIES_MIRROR=https://registry.npmmirror.com/-/binary/electron-builder-binaries/
export NODE_TLS_REJECT_UNAUTHORIZED=0
npm install
npm run electron:build
```

---

## 打包为 Windows 桌面程序（Electron）

如果你希望**不装 Node、不启服务器、双击就能用**，可以把本项目打包成一个独立的 Windows 可执行程序（`.exe`）。采用 Electron：内置浏览器内核，离线可用，可拷贝给任何人。

### 1. 安装桌面端依赖（首次）
```bash
npm install        # 会自动安装 electron / electron-builder
```

### 2. 打包
```bash
npm run electron:build
```

产物：`dist-electron/HTML-ZT-Edit Setup *.exe`（Windows 可执行安装包 / 便携版）。

---

## 自定义字幕 / 绑定 / 动画约定

如果你有现成的自动播放 HTML，想让它在编辑器中支持「绑定 + 聚焦强调动画」，可以按以下约定改造：

```html
<!-- 1. 字幕：给镜头外的字幕元素打上标识 -->
<div class="slide-subtitles" style="display:none">
  <div data-zt-role="subtitle"
       data-zt-subtitle-start="0.0"   <!-- 相对当前页起始的相对秒数 -->
       data-zt-subtitle-end="12.2">
    字幕文字
  </div>
</div>

<!-- 2. 画面元素：给要强调的元素打上唯一 id 与动画效果 -->
<div data-zt-id="el-3-1" data-zt-anim-effect="focus-zoom" class="focus-item">强调内容</div>
```

- **聚焦强调**：`data-zt-anim-effect="focus-zoom"` 的元素需放在 `focus-group` 容器内；播放到对应字幕时，同组其余 `focus-item` 变暗，目标放大高亮。
- **入场动画**：`data-zt-anim-effect="zoom-in" / fade-in / fly-left ...`，可配 `data-zt-anim-duration / delay / return / easing`。
- **绑定**：在编辑器内选中字幕 → 点「🔗 绑定」→ 点画面元素 →「✅ 确认」，会自动写入 `data-zt-bound-to="[data-zt-id='...']"`。
- 导出时编辑器会自动生成播放脚本，按原 `slideTimings` 时间轴驱动以上效果。

---

## 快捷键一览

| 按键 | 功能 |
| --- | --- |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Shift+Z` / `Ctrl+Y` | 重做 |
| `Ctrl+C` / `Ctrl+V` | 复制 / 粘贴（可跨页） |
| `Delete` / `Backspace` | 删除选中 |
| 方向键 | 微调（1px） |
| `Shift + 方向键` | 微调（10px） |
| `Shift`（拖拽缩放时） | 锁定纵横比 |
| `Alt`（框选时） | 碰边即选 |
| `Ctrl + 滚轮` | 画布缩放 |

> 焦点在输入框 / 文本域 / 下拉框内时不拦截快捷键，以便正常输入。

---

## TODO / 待实现功能

> 完整路线图（P0 出片质量三件套：录制升级 MP4 / TTS 口播产线 / 动画扩充，P1-P2 远期项）见 [WORKFLOW.md](WORKFLOW.md)，以该文档为准；本节仅保留编辑器侧长期项。

### 1. 自定义组件库

**目标**：将当前选中元素（或一组元素）保存为「组件」，后续可在任意幻灯片中一键插入，复用样式、动画、绑定关系。

**概要设计**：
- 选中元素 → 右键菜单 / 侧栏「保存为组件」→ 输入组件名称 → 存入 `localStorage`（或 `IndexedDB`）。
- 素材面板新增「组件」Tab，列出所有已保存组件（缩略图+名称）。
- 点击组件缩略图 → 画布十字光标 → 点击插入（与素材放置流程一致）。
- 保存内容：选中元素的 `outerHTML` + 其内联样式 + 子元素结构 + `data-zt-*` 属性（绑定 / 动画 / 锁定等）。
- 插入时自动生成新 `data-zt-id` 避免冲突，若组件内包含相对路径资源（图片 src），导出时需做资源映射。

### 2. ~~字幕预览播放按钮~~ —— ✅ 已实现

播放预览模式已上线：从头/本页播放、音频 seek + 字幕 + 绑定动画同步、停止返回编辑态、播放中隐藏编辑面板。

### 3. 源码双向查看面板（画布 ↔ HTML 代码对照）

**目标**：在编辑器内同时查看当前页的 HTML 源码，便于理解结构、调试样式，后续支持双向修改。

**阶段一（一期 · 只读）**：
- 侧栏新增「源码」Tab，显示当前页对应的 HTML 标记（`<div class="slide">...</div>` 内部）。
- 使用语法高亮 + 代码折叠（推荐 `CodeMirror` 或 `Prism.js` + `textarea`）。
- 选中画布元素时，同步滚动到源码中对应标签位置（通过 `data-zt-id` 或 `outerHTML` 匹配）。
- 源码只读，不可编辑（避免双向同步冲突）。

**阶段二（二期 · 双向修改）**：
- 源码区域可编辑（`CodeMirror` 提供编辑能力）。
- 修改源码 → 按 `Ctrl+S` 或点击「应用」→ 解析修改后的 HTML → 替换当前页 DOM → 保留编辑器的选中/历史堆栈。
- 冲突处理：若用户同时拖拽移动元素又修改源码，以最后一次操作为准（历史堆栈记录切换点）。
- 自动缩进 / 格式化：`Ctrl+Shift+I` 格式化当前页源码。

---

> 以上功能均为长期规划，欢迎提交 Issue 或 PR 讨论实现细节。