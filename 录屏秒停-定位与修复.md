# 录屏产出 3KB 空壳 —— 定位与修复

日期：2026-08-31
样本：`动物与道德高度演讲视觉页面_自动播放-录屏-2026-08-30-15-22-55.mp4`（2851 B）、`…-15-34-12.mp4`（2153 B）

## 一、现象与误判

弹窗报「录到了音轨，但里面是静音数据 / 音轨峰值 0.0000 / 产物含音轨：是」。

这个提示是**误导**：它把问题框定在音频上，实际根因与音频无关。

## 二、证据（解 MP4 得到的，不是猜的）

| 项目 | 实测值 |
|---|---|
| trak 数量 | **1 条，`hdlr = soun`；没有任何 `vide` 视频轨** |
| 样本数 | `stts entries=0`、`stsz sample_count=0` |
| 录制时长 | `mvhd duration` = **127 ms** / 85 ms |
| mdat | 2048 B / 1366 B |

结论：**录制在启动 100 多毫秒时就被停掉了。**
H.264/AAC 编码器初始化需要几百毫秒，来不及产出任何样本，于是
视频轨连 `trak` 都没建、音频只剩 stsd 里写着 `mp4a` 的空骨架。

弹窗里「产物含音轨：是」之所以误报，是因为旧自检用正则 `/mp4a|soun/` 匹配
——而空壳成片的 moov 里照样有这个字符串。

## 三、根因

v3.1 直接全屏录屏的时序是：
`setFullscreen(true)` → sleep 450 ms → `getDisplayMedia()`

问题在于 **`setFullScreen()` 返回时全屏并未真正稳定**：窗口样式重建 + DWM 重新合成的
余波持续数百毫秒到 1 秒。捕获正好建在余波末尾，随后失效：

```
video track ended → session.onExternalStop resolve
  → 父层 send({type:'stopPlay'}) → finishRecording() → 127ms 成片
```

127 ms 内人不可能手动停共享，所以只可能是这条自动路径。

## 四、改动

| 文件 | 改动 |
|---|---|
| `electron/main.cjs` | 新增 `waitFullscreenSettled()`：`zt:set-fullscreen(true)` 改为等 `enter-full-screen` 事件落地 + 250 ms 余量才 resolve（2 s 超时兜底；已是全屏则走 100 ms 余量） |
| `src/App.jsx` | 全屏落定后再 sleep 300 ms 才建捕获 |
| `src/App.jsx` | `onExternalStop` 加 **1.5 s 保护窗**：起步 1.5 s 内轨 ended 视为全屏余波误触发，忽略并继续录制 |
| `src/App.jsx` | 自检重写：新增 `probeTracks()`（数 vide/soun 轨 + stsz 样本数）；新增录制时长、首个数据块、MediaRecorder 错误、送入轨数；<1.5 s 直接判「过短」并改文案 |
| `src/App.jsx` | `probeAudioLevel` 在产物 <0.5 s 时不再测电平（那时「静音」是果不是因） |
| `src/recorder.js` | `stop()` 返回 `durationMs / recError / firstChunkMs`；`onExternalStop` 带出「距启动多少 ms」 |
| `src/App.jsx` | 顺手修：`probeAudioLevel` 的 AudioContext 从未 close，每次录制泄漏一个（内核约 6 个配额，录几次后就建不出来） |

## 五、验证

新 `probeTracks()` 在两个真实失败产物上的输出：
`{"video":0,"soun":1,"samples":0}` —— 而旧逻辑报「含音轨=是」。

`npm run build` 通过；已 `rm -rf dist` 清理堆积（此前堆了 3 个历史 js）。

## 六、下一步

1. 重新打包 exe（`dist-electron` 里那份是 23:33 的旧代码），或走 `npm run dev:electron` 更快。
2. 再录一次，看顶栏自检的**「录制时长」**。
3. 若「时长正常，但产物含视频轨=否」→ H.264 High profile(`avc1.640028`) 编码器不可用，
   把 `recorder.js` 的 `pickMime()` 首选降到 Baseline(`42E01E`)。
