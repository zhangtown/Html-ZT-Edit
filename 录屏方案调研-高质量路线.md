# 录屏方案调研：高质量 + 自动化路线

> 目标：为 ztEdit 的「HTML 视觉页自动播放 + 口播音频」录屏，找一个
> **清晰度有绝对保障、且能一定程度自动化** 的路线。
> 前提：清晰度不够，就不如直接 Win+Shift+R（Game Bar）自己录。

## 0. 当前 v3.3 的痛点本质

v3.3 在 Electron 内走 `getDisplayMedia → canvas → WebCodecs(视频) + MediaRecorder(音频)`。
调研证实 Electron 桌面捕获基于 Chromium WebRTC 管线（为视频通话设计，非录屏）：

- 帧率封顶 30fps（传 60 被忽略），4K 下 Chromium 自动节流到 15fps
- 走 canvas 缩放引入清晰度损耗
- 自己编码（WebCodecs/MediaRecorder）占主线程 → 音频回调被饿死（整轮踩坑的根因）
- 音画同源、AudioContext 采样率、AAC 编码等一堆坑都源于「在 Electron 里自己编码」

**结论**：要清晰度和自动化都稳，应把「捕获 + 编码」交给专业系统级工具，
Electron（ztEdit）只负责 UI 调度与「开始/停止录制」的触发。

## 1. 为什么系统级工具更清晰

专业录屏走 OS 原生捕获（Windows: DXGI Desktop Duplication / Windows.Graphics.Capture）
→ 直连硬件编码器（NVIDIA NVENC / Intel QSV / x264），绕过 Chromium：

- 原生分辨率直出（2K/4K，零缩放零黑边，对标 Game Bar）
- 高码率 40–60Mbps（Game Bar 默认中等码率，轻松超过）
- 硬件编码近无损（CRF/CQP 18~23，或 QP 0）

## 2. 方案对比

| 方案 | 清晰度 | 自动化 | 音频 | 额外依赖 | 适合度 |
|---|---|---|---|---|---|
| **A. OBS + obs-websocket** | ★★★★★ 硬件编码高码率 | ★★★★ `obs-cmd` 一行启停 | ★★★★★ 默认抓「桌面音频」，开箱即用 | 装 OBS(~100MB) | **最省心** |
| **B. ffmpeg gdigrab/ddagrab** | ★★★★★ 同左，更轻量 | ★★★★ bat/ps1 脚本 | ★★★ 抓系统声需 Stereo Mix / VB-CABLE | 带 ffmpeg（曾为省 80MB 放弃） | 最脚本化 |
| **C. WebVideoCreator / Playwright 帧渲染** | ★★★★★ 像素级完美（程序生成非录制） | ★★★★★ npm 批量/定时 | ✗ 不支持实时 WebAudio 口播 | Chromium + Puppeteer | **不适用**（需改生成端） |
| **D. 维持 v3.3** | ★★★★ 15–16Mbps CBR | ★★★★★ 内建 | 已修好但链路脆弱 | 无 | 当前可用 |

> 关键差异：OBS 在 Windows 上**默认就有「桌面音频」源**，系统出声即录到，
> 不需要虚拟设备；ffmpeg 抓系统声要开 Stereo Mix 或装 VB-CABLE，稍麻烦。

## 3. 推荐方案 A：OBS 集成到 ztEdit

**一次性配置（GUI）**
1. 装 OBS Studio（obsproject.com）。
2. 场景来源 = 「窗口捕获」选 ztEdit 主窗口，或「显示器捕获」；音频 = 「桌面音频」（默认存在）。
3. 设置 → 输出 → 录制：编码器 `NVIDIA NVENC H.264`，速率控制 `CQP`=18（或 `CBR` 50Mbps），
   分辨率「与窗口/显示器相同」（2K 直出），格式 MP4。
4. 工具 → WebSocket 服务器设置：启用，设密码（记住端口 4455）。

**自动化（CLI，无需 GUI 操作）**
- `obs-cmd`（Rust CLI，obs-websocket v5，github.com/grigio/obs-cmd）：
  `obs-cmd recording start` / `obs-cmd recording stop`
- ztEdit 主进程在「开始播放」时 `child_process.spawn('obs-cmd', ['recording','start'])`；
  播放结束（已知 `subtitles[]` 总时长）调 `recording stop`。
- 也可 OBS 内设「录制时长限制」或用热键，但 CLI 触发最贴合 ztEdit 的播放时序。

**优势**：彻底绕开 WebCodecs 音频链路所有坑；清晰度由 NVENC 保证，远超 Game Bar。
**改动量**：仅改主进程「开始/停止录制」逻辑，播放/字幕/动画引擎完全不动。

## 4. 备选方案 B：ffmpeg 集成

**命令示例（Win11 用 ddagrab 优于 gdigrab）**
```
ffmpeg -y -f gdigrab -framerate 30 -i desktop ^
  -f dshow -i audio="立体声混音 (Realtek)" ^
  -c:v h264_nvenc -qp 0 -c:a aac out.mp4
```
- `-qp 0` ≈ 近无损；有 NVENC 用 `h264_nvenc`，否则 `libx264 -crf 18`。
- 4K60 用 `ddagrab + h264_nvenc` 轻松胜任。
- 音频抓系统声：需开启「立体声混音」或装 VB-CABLE（比 OBS 多一步）。

**集成**：ztEdit 主进程 spawn 一个 bat/ps1 启停 ffmpeg；参数（分辨率/码率/输出路径）预置好。

## 5. 方案 C 为何不适用（留作终极方向）

WebVideoCreator（github.com/kx0101/WebVideoCreator）等是「逐帧渲染 HTML → ffmpeg 编码」，
像素级完美、可批量。但要求动画用 `renderFrame(t)` 而非 CSS @keyframes / rAF 做帧级 seek；
且**不支持实时 WebAudio 口播音频捕获**（需预先合成音轨）。
你的 HTML 是手写 CSS 动画 + WebAudio 口播 + 字幕绑定，迁移成本极高。
→ 若未来要「生成端直接出视频」，应在 `speech-visual-html` 技能侧做，不在 ztEdit 侧。

## 6. 与现有 v3.3 的关系

- v3.3 已修好（WebCodecs 视频 + MediaRecorder 音频，交错封装），可继续用。
- 但 OBS/ffmpeg 路线：① 彻底绕开音频坑；② 清晰度上限更高；③ 长期更稳（专业工具维护）。
- 实施只需改 ztEdit 主进程「开始/停止录制」触发点，其余不动。

## 7. 参考

- OBS Studio: https://obsproject.com/ ；obs-cmd: https://github.com/grigio/obs-cmd
- FFmpeg 桌面捕获: https://trac.ffmpeg.org/wiki/Capture/Desktop （gdigrab / ddagrab）
- WebVideoCreator: https://github.com/kx0101/WebVideoCreator
- 「Why I Rewrote My Screen Recorder from Electron to Native」(Snapr) —— 印证 Electron 自带录屏的结构性局限
- 火山引擎: FFmpeg 实现与 GeForce Experience 相当的录屏性能（NVENC 参数范例）
